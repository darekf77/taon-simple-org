
const path = require('path')
    var { config } = { config: {} };

    config = {

      domain: 'third.example.domain.com',
      useDomain: false,
      title: 'Third',
      pwa: {
        // start_url: ''
      }

    }
    module.exports = exports = { config };
    