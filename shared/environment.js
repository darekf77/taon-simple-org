
const path = require('path')
    var { config } = { config: {} };

    config = {

      domain: 'shared.example.domain.com',
      useDomain: false,
      title: 'Shared',
      pwa: {
        // start_url: ''
      }

    }
    module.exports = exports = { config };
    