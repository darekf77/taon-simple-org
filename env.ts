import type { EnvOptions } from 'tnp/src';

const env: Partial<EnvOptions> = {
  website: {
    domain: 'taon-simple-org.example.domain.com',
    title: 'Taon Simple Org',
  },
};
export default env;
