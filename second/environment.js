
const path = require('path')
var { config } = { config: {} };

config = {

    domain: 'second.example.domain.com',
    useDomain: false,
    title: 'Second',
    pwa: {
        // start_url: ''
    }

}
module.exports = exports = { config };
